<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.BurningUp">
    <!-- 인터넷 권한 -->
    <uses-permission android:name="android.permission.INTERNET" /> <!-- camera, gallery -->
    <uses-permission android:name="android.permission.CAMERA" /> <!-- 카메라 권한 -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" /> <!-- 파일 읽기 권한 -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" /> <!-- 파일 쓰기 권한 -->
    <uses-feature
        android:name="android.hardware.camera"
        android:required="true" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_burning_up"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_burning_up_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">

        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />


        <activity android:name=".AddChatRoomActivity" />
        <activity
            android:name=".NoticeActivity"
            android:windowSoftInputMode="adjustPan" />
        <activity android:name=".VotePostingActivity"
            android:windowSoftInputMode="adjustPan"/>

        <activity
            android:name="com.theartofdev.edmodo.cropper.CropImageActivity"
            android:theme="@style/Base.Theme.AppCompat" />

        <activity android:name=".ChangePasswordActivity"></activity>
        <activity
            android:name=".IntroduceProgrammerFragment"
            android:label="@string/title_activity_introduce_programmer_fragment"
            android:theme="@style/NoActionBar"/>
        <activity
            android:name=".AlarmSettingFragment"
            android:label="@string/title_activity_alarm_setting_fragment"
            android:theme="@style/NoActionBar" />
        <activity android:name=".ChangePasswordFragment" />
        <activity android:name=".ChangeProfileFragment" />
        <activity android:name=".SsivalActivity" />

        <activity android:name=".Register1Activity" />
        <activity android:name=".Register2Activity" />
        <activity android:name=".LoginActivity" />

        <activity
            android:name=".CustomDialog"
            tools:ignore="Instantiatable" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="com.example.BurningUp.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>

</manifest>