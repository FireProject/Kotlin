1. xml 최상위 레이아웃에 onClick 등록

2. var imm : InputMethodManager? = null

3. imm = getSystemService(INPUT_METHOD_SERVICE) as InputMethodManager?

4.fun hideKeyboard(v:View){
        if(v!=null){
            imm?.hideSoftInputFromWindow(v.windowToken,0)
        }
    }